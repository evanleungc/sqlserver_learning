select * from tblGender
select * from tblPerson

Delete from tblGender Where ID=2 --Get error at first, after alter the delete action to default, it will replace with default
--Delete options -Cascade, default, null